{
  "name": "iitc-plugin-follow-mode",
  "version": "0.2.13-dev",
  "private": true,
  "description": "IITC add-on that improves IITC User Location with smoothed, heading-up follow movement.",
  "scripts": {
    "build": "node build.js",
    "clean": "rm -rf dist && mkdir -p dist",
    "dev": "npm run build && python3 -m http.server 8000",
    "check": "npm run build && node --check dist/follow-mode.user.js"
  },
  "engines": {
    "node": ">=18"
  }
}
