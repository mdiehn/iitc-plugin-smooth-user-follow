{
  "name": "iitc-follow-mode",
  "version": "0.2.3-dev",
  "private": true,
  "description": "IITC plugin that adds smooth, heading-up user-location follow mode.",
  "scripts": {
    "build": "node build.js",
    "clean": "rm -rf dist && mkdir -p dist",
    "dev": "npm run build && python3 -m http.server 8000"
  },
  "engines": {
    "node": ">=18"
  }
}
